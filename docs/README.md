# Docs

- [`github-profile-readme-fragment.html`](github-profile-readme-fragment.html) — README-style HTML you provided (remote `img` URLs unchanged).

To download those assets into `public/readme-assets/` and emit a local-copy HTML file, run from the repo root:

```bash
node scripts/fetch-readme-assets.mjs
```
